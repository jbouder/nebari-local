apiVersion: v2
# Vendored from the PUBLISHED nebari-frames 0.1.7 chart (helm pull
# nebari/nebari-frames --version 0.1.7), not from git: CI stamps version and
# appVersion at release time, so the in-repo chart/Chart.yaml still reads 0.1.0.
# Version left at the upstream release value — the only local delta is the
# orgCABundle addition in values.yaml + templates/deployment.yaml.
appVersion: v0.1.7
description: Nebari Frames - single-binary Frames server (SPA + API) for Nebari
name: nebari-frames
type: application
version: 0.1.7
