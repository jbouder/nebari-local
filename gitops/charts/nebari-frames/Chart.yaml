apiVersion: v2
# Vendored from nebari-frames main at b8db7f5 (PR #56, runtime branding), which
# is AHEAD of the v0.1.5 release — branding has not been released yet. The
# prerelease version string records that provenance; re-stamp it to the plain
# release version once a tag carrying branding exists upstream.
appVersion: sha-b8db7f5
description: Nebari Frames - single-binary Frames server (SPA + API) for Nebari
name: nebari-frames
type: application
version: 0.1.6-main.b8db7f5
