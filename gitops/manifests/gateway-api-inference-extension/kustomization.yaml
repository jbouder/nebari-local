apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CRDs only (InferencePool, InferenceObjective, ...) — the llm-serving
# operator informs on InferencePool at startup and crashloops without them.
# Version from the llm-serving-pack installation docs.
resources:
  - https://github.com/kubernetes-sigs/gateway-api-inference-extension/releases/download/v1.5.0/manifests.yaml
